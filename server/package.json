{
  "name": "server",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "nodemon": "^2.0.22",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "scripts": {
    "build": "yarn tsc",
    "start": "yarn run build && nodemon build/server.js"
  }
}